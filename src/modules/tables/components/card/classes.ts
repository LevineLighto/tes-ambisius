export const TableCardClasses = {
    background : {
        empty       : '!bg-slate-200',
        occupied    : 'bg-white',
    }
}

export const CustomerClasses = {
    margin: 'mb-3',
}

export const CounterClasses = {
    weight: 'font-semibold',
}